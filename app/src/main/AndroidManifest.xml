<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.georeminder">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="georeminder.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="georeminder.MainActivity">
        </activity>
        <activity android:name="georeminder.AddNoteActivity"></activity>
        <activity android:name="georeminder.AddGeoActivity" />
        <activity android:name="georeminder.AddRoutineActivity" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDfypgml757c_nk7o2fjgd05pfJhCZAnFA"/>

        <receiver
            android:name="broadcast.RestarterBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="restartservice" />
            </intent-filter>
        </receiver>
        <receiver android:name="broadcast.Alarm" />

        <service
            android:name="services.RemindersService"
            android:enabled="true" >
        </service>

        <service
            android:name="services.GeoService"
            android:enabled="false" >
        </service>

        <service
            android:name="services.RoutineService"
            android:enabled="false" >
        </service>

    </application>

</manifest>